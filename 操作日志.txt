搞懂了 database 和 table;
搞懂了 table里面的 各个'栏位' 和 属性; modify / change
搞懂了添加元组(数据条)insert, 删除 delete，改动 update
2025.01.02

练习巩固了 group by... having 的用法，知道了前面select的栏位 必须也要在后面的 group by 中出现(如果select多个栏位)。但是聚合函数()就不用了，它会自行出现在后面group by的展示中
学会了Order by，升asc/降序desc排序
学会了简单的综合运用查询语句，以及简单的创建删除新database用户
2025.01.03

学会了怎么设置用户权限，具体到某一个数据库的某一张表 (或是直接用* 代替全部[表/databases])
学会了mysql内置的常用字符串函数(),如 l/rpad()填充 ，upper() lower()大小写，substring() 截取 , concat()拼接, trim()删首尾空格等...
学会了综合运用 数值函数()
学会了综合运用 时间日期函数()
学会了综合运用 流程控制函数()
2025.01.04

学会了 创建/更改栏位的'各种约束', primary key主键, unique 独特键(primary key备选), foreign key 外表主键等...
学会了 针对'外键变动'时的约束，On update 主表主键更新时要干什么, On Delete 主表主键删除时要干什么
2025.01.05

搞明白了多表之间的连接关系，一对一 1 to 1，一对多 1 to N，多对多 N to N，实践花了半天的时间
2025.01.06

巩固了一下多表查询，初步学习了内/外 还有 自连接
学会了联合查询，搞懂了union 和 union all的区别
初步认识了子查询，明白了子查询的四种分类
2025.01.07

系统的了解了各种子查询，并进行了相应的实践操作，又是花了半天的时间
综合的进行复杂查表的自我训练，现阶段最直接的idea就是把'各个小表拼成一张巨大的表'，然后直接针对这个大表的所有栏位进行相应的筛选 和 操作，滤除想要的结果 （核心思路是: 提前把表拼好XD）
2025.01.08

简单的认识了下'事务'，即表示一个'操作集合'(一次性全成后commit 或 出错及时rollback，都标志着一个事务的结束)
认识了事务的ACID特性，同时形象的用笔记记录了常见的并发事务问题，差事务隔离还没有看；同时猛地回想起之前上课好像有讲过这块，但是记得笔记太偏理论了xwx...
2025.01.09

看完了事务隔离，同时回忆了一下之前课上讲的'冲突可串行化'，了解了各种不同的事务隔离级别(事务隔离级别越高，安全性越好[自动锁事务]，但是代价是性能糟糕)
初步的了解了MySQL的体系结构，还有'存储引擎'，存储引擎就是存储数据、建立索引、更新/查询数据等技术的实现方式(存储引擎是基于'表'的)
了解了InnoDB, MyISAM 和 Memory这三种不同的存储索引，经常修改的用InnoDB, 经常插入读取用MyISAM(少改)，玩点狠活往内存里塞的用Memory
2025.01.10